/* Supabase 配置（可选）。为空则纯本地模式。
   设置方法：在 supabase 官网建项目 → 获取 Project URL 与 anon public key，填入下方。
   然后按 README 在 SQL Editor 运行建表脚本。 */
window.FOCUSPLAY_CONFIG = {
  url: "",          // 例如 https://xxxx.supabase.co  （留空 = 关闭同步）
  anonKey: ""       // anon / public 密钥
};